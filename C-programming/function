#include <stdio.h>

// Function declaration with void return type
void add(int a, int b) {
    int sum = a + b;
    printf("Sum: %d\n", sum);
}

int main() {
    int num1 = 5, num2 = 10;

    // Call the add function
    add(num1, num2);

    return 0;
}